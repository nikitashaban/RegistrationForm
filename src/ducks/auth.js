// const

//initial state

const initialState = {
  a: 100
};

//reducer
export default (state = initialState, { type, payload }) => {
  switch (type) {
    default: {
      return state;
    }
  }
};

//actions

//action creators
